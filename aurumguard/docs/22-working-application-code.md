# 22 Working application code

All code is in the repository. Entry points: `backend/app/main.py` (API + scheduler), `backend/app/seed.py` (demo environment), `frontend/src/app` (pages). Stage-by-stage implementation notes, including what was executed and what was validated logically only, are collected in `36-production-readiness-assessment.md` (section "Verification record").
